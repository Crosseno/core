# Contributing

Install dependencies with `composer install`, make focused changes, and run `composer check`. Public API changes require an ADR when they alter package boundaries, snapshot compatibility, determinism, or resource-limit behavior. Keep commits free of generated vendor files and unrelated formatting changes.
